# Copy to .env.local and fill in the values

# Better Auth - Secret key for authentication (min 32 characters)
BETTER_AUTH_SECRET=

# Site URL - The URL where your frontend is running
SITE_URL=http://localhost:3001

# Convex URLs - Auto-provided by Convex but set here for validation
# Get these from your Convex dashboard or after running `npx convex dev`
CONVEX_CLOUD_URL=
CONVEX_SITE_URL=

# Note: CONVEX_DEPLOYMENT is auto-generated by `npx convex dev`

# Email Configuration
# Option 1: Resend API (production)
# Get from https://resend.com/api-keys
RESEND_API_KEY=

# Option 2: Local SMTP with smtp4dev (development)
# Run: docker run -d -p 5080:80 -p 2525:25 rnwood/smtp4dev
# View emails at: http://localhost:5080
USE_SMTP=true
SMTP_HOST=localhost
SMTP_PORT=2525
